import View from "../view.js";

let myInfo = {
    name: "myInfo",
    template: `
    <section class="myInfo">
        MyInfo
    </section>
    `
};

myInfo = new View(myInfo);
export default myInfo;